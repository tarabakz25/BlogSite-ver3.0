---
const { skillName } = Astro.props;
---
<div class="skill-box">
    <span>{ skillName }</span>
</div>

<style>
    .skill-box {
        margin: 30px 60px;    
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px 40px;
        border: 2px solid white;
        position: relative;
        color: white;
        font-family: 'Anonymous pro', sans-serif;
        font-size: 24px;
        width: 150px;
        height: 30px;
    }

    .skill-box span {
        margin-bottom: 5px;
        text-align: center;      /* 追加: テキストを中央揃え */
        text-align-last: center; /* 追加: 最後の行も中央揃え */
        width: 100%;            /* 追加: spanを親要素いっぱいに広げる */
        line-height: 1.2;       /* 追加: 行間の調整 */
    }

    .skill-box::before,
    .skill-box::after {
        content: '';
        position: absolute;
        border: 2px solid white;
        width: 100%;
        height: 100%;
        top: -10px;
        left: -10px;
        z-index: -1;
    }
</style>